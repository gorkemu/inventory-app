doctype html
html(lang='en')
  head
    title= title
    meta(charset='utf-8')
    meta(name='viewport', content='width=device-width, initial-scale=1')
    link(rel='stylesheet', href='/stylesheets/style.css')
  body
    div(class='container')
      nav(class='navbar')
        ul(class='navbar-nav-left')
          li(class='nav-item')
            a(href='/catalog' class='nav-link') Home
          li(class='nav-item')
            a(href='/catalog/categories' class='nav-link') Categories
          li(class='nav-item')
            a(href='/catalog/products' class='nav-link') Products
        ul(class='navbar-nav-right') 
          li(class='nav-item nav-item-right')
            a(href='/catalog/category/create' class='nav-link') Add category
          li(class='nav-item nav-item-right')
            a(href='/catalog/product/create' class='nav-link') Add product
      main(class='col-sm-10')
        block content